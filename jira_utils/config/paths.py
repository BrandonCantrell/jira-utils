from pathlib import Path

CONFIG_PATH = Path.home() / ".jira_cli_config.json"
KEY_PATH = Path.home() / ".jira_cli_key.key"
LOG_PATH = Path.home() / ".jira_cli_error.log"
CATEGORY_PATH = Path.home() / ".jira_cli_categories.json"
